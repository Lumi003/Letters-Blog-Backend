# Copy this to .env for local development
# On your host (Render/Railway), set these as environment variables

# The URL of your deployed frontend — used for CORS
FRONTEND_URL=https://your-blog.vercel.app

# Port — your host sets this automatically, no need to change
PORT=3001
